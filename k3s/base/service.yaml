apiVersion: v1
kind: Service
metadata:
  name: okard-frontend
  labels:
    app: okard-frontend
spec:
  selector:
    app: okard-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
