apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ingress.yaml

namePrefix: prd-
namespace: prd

patches:
  - path: deployment-patch.yaml

images:
  - name: okard-frontend
    newName: ghcr.io/o-kard/okard-frontend
    