apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: okard-frontend
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
    - host: uat.081246.xyz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: okard-frontend-svc
                port:
                  number: 80
